#include "graphicsobserver.h"

GraphicsObserver::GraphicsObserver(Game &game): game(game) {
    // xw.fillRectangle(0, 0, 500, 500, Xwindow::White);
}

void GraphicsObserver::drawBoard() {

}

GraphicsObserver::~GraphicsObserver() = default;
